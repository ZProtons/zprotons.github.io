on:
  push

name: ci

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    - uses: docker-practice/actions-setup-docker@master
      timeout-minutes: 12
    - run: |
        set -x

        docker version
        docker run -d --restart unless-stopped --name alpha-video -p 5000:5000 -e subdomain=sultanzayed098412321 andrewstech/alpha-video:latest
        docker logs 10029b35764ccbb3743e41514a5e88a58d185e08b52f86d7caea73e66132a9a5
        
