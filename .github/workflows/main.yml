name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Running
      run: docker run -d --restart unless-stopped --name alpha-video -p 5000:5000 -e subdomain=sultanzayed098432 andrewstech/alpha-video:latest
      
    
